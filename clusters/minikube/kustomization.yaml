apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: my-app
  namespace: flux-system
spec:
  interval: 1m
  path: ./clusters/minikube
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-learning
  targetNamespace: default  # Ensure deployment is in the correct namespace
